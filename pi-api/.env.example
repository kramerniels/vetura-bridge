# Copy to .env and fill in values. Never commit .env to git.
API_SECRET=change-me-to-a-long-random-string
HOST=127.0.0.1
PORT=3000

# Optional: comma-separated list of allowed client IPs (useful without Cloudflare Tunnel)
# ALLOWED_IPS=203.0.113.10,198.51.100.5

# Optional overrides
# RATE_LIMIT_MAX=30
# SCRIPT_TIMEOUT_MS=30000
